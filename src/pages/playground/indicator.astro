---
import Layout from '@/layouts/Layout.astro';
import ChatIndicator from '@rcomp/playground/nanostores/ChatIndicator';
import { createSeo } from '@/lib/shared/schemas/seo/seo.schema';
const seo = createSeo({
  title: 'Test Indicator',
  description: 'Powered by Astro + Hono + oRPC',
  type: 'website',
});
---

<Layout {...seo}>
  # Online or offline indicator
  <ChatIndicator client:load slot="main" />
</Layout>
